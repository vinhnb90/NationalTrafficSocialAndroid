<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/edit_profile_label_email" />

    <EditText
        android:id="@+id/value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/label"
        android:layout_marginEnd="@dimen/edit_profile_dropdown"
        android:layout_marginRight="@dimen/edit_profile_dropdown"
        android:layout_toEndOf="@+id/label"
        android:layout_toLeftOf="@+id/img_dropdown"
        android:layout_toRightOf="@+id/label"
        android:layout_toStartOf="@+id/img_dropdown"
        android:background="@android:color/transparent"
        android:gravity="end"
        android:inputType="text"
        android:textColor="@color/black" />

    <TextView
        android:id="@+id/value1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/label"
        android:layout_toEndOf="@+id/label"
        android:layout_toLeftOf="@+id/img_dropdown"
        android:layout_toRightOf="@+id/label"
        android:layout_toStartOf="@+id/img_dropdown"
        android:background="@android:color/transparent"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="end"
        android:textColor="@color/black"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/img_dropdown"
        android:layout_width="@dimen/edit_profile_dropdown"
        android:layout_height="@dimen/edit_profile_dropdown"
        android:layout_alignBottom="@+id/value1"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/common_image"
        android:visibility="gone"
        app:srcCompat="@drawable/ic_arrow_drop_down_black_48dp" />
</RelativeLayout>